services:
  clubsandsocs-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clubsandsocs-api
    hostname: clubsandsocs-api
    ports:
      - "4000:4000"
    restart: unless-stopped
